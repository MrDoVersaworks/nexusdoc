{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "express",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run db:migrate; else echo 'Skipping production database migration for preview deployment'; fi",
  "crons": [
    {
      "path": "/api/internal/storage-cleanup",
      "schedule": "0 3 * * *"
    }
  ]
}
